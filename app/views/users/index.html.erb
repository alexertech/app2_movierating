<% @users.each do |u| %>

    <%=link_to 'e', edit_user_path(u)%> |
    <%=link_to 'd', user_path(u),
    method: :delete,
    data: { confirm: 'Are you sure?' } %> |
    <%=u.email %> 
    <br>
<% end %>
<br><br>
<%= link_to 'New', new_user_path %>